# mobileTestApp

Monorepo con la aplicación completa, dividido en dos proyectos independientes:

- [`frontend/`](frontend/README.md) — aplicación React Native (Android/iOS).
- [`backend/`](backend/README.md) — servicio backend.

Cada carpeta tiene su propio `package.json`, dependencias y `.gitignore`. Consulta el README de cada una para instrucciones específicas de instalación y ejecución.
